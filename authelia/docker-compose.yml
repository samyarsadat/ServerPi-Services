# ServerPi Services - Authelia Compose File
# Prepared by Samyar Sadat Akhavi, 2024. 

name: "authelia"
services:
    # AUTHELIA SERVICE
    authelia:
        image: authelia/authelia:4.39.20
        container_name: authelia
        hostname: authelia
        restart: unless-stopped
        env_file:
            - .env
        depends_on:
            - database
            - redis
        volumes:
            - ./config:/config

    # POSTGRES DATABASE FOR AUTHELIA
    database:
        image: postgres:18
        container_name: authelia-database
        hostname: authelia-database
        restart: unless-stopped
        env_file:
            - .env
        volumes:
            - ./postgres:/var/lib/postgresql
            - ./config/secrets/STORAGE_PASSWORD:/STORAGE_PASSWORD:ro

    # REDIS CACHE FOR AUTHELIA
    redis:
        image: redis:8
        container_name: authelia-redis
        hostname: authelia-redis
        restart: unless-stopped
        env_file:
            - .env
        entrypoint: ["/bin/sh", "/redis-entry.sh"]
        volumes:
            - ./redis:/data
            - ./redis-entry.sh:/redis-entry.sh
            - ./config/secrets/REDIS_PASSWORD:/REDIS_PASSWORD:ro

networks:
    default:
        name: $DOCKER_MY_NETWORK
        external: true