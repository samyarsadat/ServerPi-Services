apiVersion: 1
providers:
    - name: ServerPi
      type: file
      folder: ServerPi
      allowUiUpdates: true
      options:
          path: /var/lib/grafana/dashboards
          foldersFromFilesStructure: false